# E2E Tests

Playwright smoke and e2e scenarios live here.

Current baseline:

- `foundation-smoke.spec.ts` checks app startup
- verifies public shell rendering
- verifies `/api/directions`
- verifies internal dashboard access through dev auth
